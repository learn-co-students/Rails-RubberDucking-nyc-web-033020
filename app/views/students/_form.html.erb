<% if @errors %>
  <% @errors.each do |error| %>
    <%= error %><br>
  <% end %>
<% end %>

<%= form_for(@student) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :mod %>
  <%= f.text_area :mod %><br>

  <%= f.submit %>
<% end %>
